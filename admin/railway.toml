[build]
builder = "DOCKERFILE"
dockerfilePath = "../Dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn admin.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3